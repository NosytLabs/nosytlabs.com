---
import type { CardProps } from '../ui/card';
import { Card } from '../ui/card.tsx';

export interface Props extends CardProps {
  class?: string;
}

const { ...rest } = Astro.props;
---

<Card client:load {...rest}>
  <slot />
</Card>
