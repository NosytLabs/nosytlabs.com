---
const processSteps = [
  {
    title: "Quick Discovery Call",
    description: "Define goals, constraints, and success criteria. Pick the shortest path to a useful outcome.",
    icon: "search",
  },
  {
    title: "Scope & Plan",
    description: "Agree on deliverables, timeline, and communication cadence. Streamlined PM—no heavy overhead.",
    icon: "pen-tool",
  },
  {
    title: "Build & Iterate",
    description: "Rapid implementation with async updates, short feedback loops, and early demos.",
    icon: "code",
  },
  {
    title: "Review & Handoff",
    description: "Ship, document, and hand off. Optional support window for minor tweaks and questions.",
    icon: "rocket",
  },
];
---

<section class="bg-secondary py-20">
<div class="container">
       <div class="text-center mb-12 animate-fade-in-up">
         <h2 class="text-h2 bg-gradient-to-r from-primary via-accent to-primary bg-clip-text text-transparent animate-gradient-shift">How We Work</h2>
         <p class="text-lg text-muted-foreground mt-2">
           A streamlined approach to delivering exceptional results with minimal overhead.
         </p>
       </div>
       <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
         {processSteps.map((step, index) => 
           <div class="process-step bg-card p-6 rounded-lg border border-border text-center relative hover:border-primary/50 transition-all duration-300 group overflow-hidden">
             {/* Animated background gradient */}
             <div class="absolute inset-0 bg-gradient-to-br from-primary/3 via-transparent to-accent/3 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
             
             {index < processSteps.length - 1 && (
               <div class="hidden lg:block absolute top-1/2 -right-4 transform -translate-y-1/2 z-10">
                 <svg class="w-8 h-8 text-primary group-hover:animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                   <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                 </svg>
               </div>
             )}
             <div class="relative z-10">
               <div class="flex justify-center items-center mb-4">
                 <div class="w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center group-hover:bg-primary/20 transition-colors duration-300">
                   <span class="text-primary font-bold text-lg">{index + 1}</span>
                 </div>
               </div>
               <h3 class="text-xl font-bold mb-2 group-hover:text-primary transition-colors duration-300">{step.title}</h3>
               <p class="text-muted-foreground text-sm">{step.description}</p>
             </div>
           </div>
         )}
       </div>
     </div>
   </section>

